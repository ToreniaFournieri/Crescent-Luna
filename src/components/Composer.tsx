export function Composer({ text, complete, locked, onUndo, onSend }: { text: string; complete: boolean; locked: boolean; onUndo: () => void; onSend: () => void }) {
  return <div className="composer"><div className="draft" role="textbox" aria-readonly="true" aria-label="Message being composed"><span>{text}</span>{!complete && <span className="cursor" />}</div>{complete && <button className="undo-selection" aria-label="Undo selection" title="Choose a different response" disabled={locked} onClick={onUndo}><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M21 5H8l-6 7 6 7h13V5Z" /><path d="m10 9 6 6m0-6-6 6" /></svg></button>}<button className="send" aria-label="Send message" disabled={!complete || locked} onClick={onSend}>↑</button></div>
}
